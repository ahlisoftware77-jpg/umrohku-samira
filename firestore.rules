rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive rules for CMS Landing Page Builder
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
